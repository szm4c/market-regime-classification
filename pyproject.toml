[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "market-regime"
version = "0.1.0"
description = "Market regime classification on TTF futures."
requires-python = ">=3.13"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
